/**
 * Online co-op: paste your Supabase project URL and anon (public) key.
 * Create a free project at https://supabase.com → Settings → API.
 * Enable Realtime: Project Settings → API → Realtime (enabled by default on new projects).
 *
 * Leave empty to hide online features until configured.
 */
window.NETPLAY_SUPABASE_URL = "";
window.NETPLAY_SUPABASE_ANON_KEY = "";
